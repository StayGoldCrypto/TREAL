[package]
name = "rsearcher"
version = "0.1.0"
edition = "2021"

[dependencies]
walkdir = "2.3.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ignore = "0.4"  # Replace with the latest version if needed

